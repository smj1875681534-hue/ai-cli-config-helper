model = "provider-model"
model_provider = "relay"

[model_providers.relay]
name = "Relay"
base_url = "https://relay.example.com/v1"
# Fake test key. This is not a real API key.
# Used only to verify secret redaction behavior.
env_key = "sk-testabcdefghijklmnopqrstuvwxyz123456"
